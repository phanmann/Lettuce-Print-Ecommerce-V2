'use client';
import { FormEvent, useState } from 'react';
export default function QuotePage() {
  const [message, setMessage] = useState('');
  async function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const formData = new FormData(event.currentTarget); const payload = Object.fromEntries(formData.entries()); const response = await fetch('/api/quote', { method:'POST', headers:{ 'Content-Type':'application/json' }, body: JSON.stringify(payload) }); if (response.ok) { setMessage('Quote request submitted. Replace the API route email logic when you are ready for production notifications.'); event.currentTarget.reset(); } }
  return <main className="section"><div className="container"><h1 className="display" style={{fontSize:'clamp(42px,8vw,82px)',margin:'0 0 16px'}}>Request a custom quote</h1><p className="eyebrow" style={{maxWidth:720}}>Use this path for packaging, installs, multi-piece campaigns, or any job that should not be squeezed into a quick-order product configurator.</p><form className="card" style={{padding:24,marginTop:24}} onSubmit={handleSubmit}><div className="form-grid"><label>Full name<input required name="name" /></label><label>Email<input required type="email" name="email" /></label><label>Phone<input name="phone" /></label><label>Project type<select required name="projectType"><option value="">Select one</option><option>Packaging</option><option>Signage / Install</option><option>Brand launch kit</option><option>Graphic design</option><option>Other</option></select></label><label>Quantity<input name="quantity" placeholder="Ex. 500, 1,000, or mixed" /></label><label>Deadline<input name="deadline" type="date" /></label><label style={{gridColumn:'1 / -1'}}>Specs<textarea name="specs" placeholder="Dimensions, material, finish, special notes, and any requirements." /></label><label style={{gridColumn:'1 / -1'}}>Artwork link or file notes<input name="fileUrl" placeholder="Paste a Drive link, Dropbox link, or explain what files you have." /></label></div><div style={{marginTop:18}}><button className="button button-primary">Submit quote request</button></div>{message ? <p className="notice" style={{marginTop:18}}>{message}</p> : null}</form></div></main>;
}
