export default function FaqPage() { const faqs = [['Can I upload artwork later?','Yes. The checkout flow is intentionally designed so artwork does not block conversion for simple jobs.'],['Which products should be shoppable?','Standard items with repeatable specs: cards, flyers, stickers, roll labels, and simple banners.'],['When should customers use quote?','Anything with unusual dimensions, materials, packaging requirements, installs, or complex timelines.']]; return <main className="section"><div className="container"><h1 className="display" style={{fontSize:'clamp(42px,8vw,82px)'}}>FAQ</h1><div className="table-list" style={{marginTop:24}}>{faqs.map(([question, answer]) => <div key={question} className="card" style={{padding:24}}><strong>{question}</strong><p className="eyebrow">{answer}</p></div>)}</div></div></main>; }
