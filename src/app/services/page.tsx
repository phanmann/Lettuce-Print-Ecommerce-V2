export default function ServicesPage() { const services = ['Business cards and stationery','Flyers, postcards, and promo pieces','Stickers and roll labels','Vinyl banners and event signage','Packaging support and print design','Rush jobs and local NYC delivery']; return <main className="section"><div className="container"><h1 className="display" style={{fontSize:'clamp(42px,8vw,82px)'}}>Services</h1><div className="steps" style={{marginTop:24}}>{services.map((service) => <div key={service} className="card" style={{padding:24}}><strong>{service}</strong></div>)}</div></div></main>; }
